import React, { Component, PropTypes } from 'react';
import styles from './';

export default class Project extends Component {
    static propTypes = {

    }

    render() {
        return (
            <div style={styles}>
                wtf
            </div>
        );
    }
}
